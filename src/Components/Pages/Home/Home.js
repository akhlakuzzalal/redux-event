import React from 'react';
import Banner from '../../Section/Banner/Banner';

const Home = () => {
    return (
        <main>
            <Banner></Banner>
        </main>
    );
};

export default Home;